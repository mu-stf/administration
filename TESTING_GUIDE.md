# 🧪 دليل اختبار نظام إدارة المحل

## ✅ قائمة الاختبار الشاملة

### المرحلة 1: التسجيل وتسجيل الدخول

#### 1.1 التسجيل (register.html)
- [ ] فتح `register.html` في المتصفح
- [ ] ملء النموذج:
  - البريد: `admin@test.com`
  - كلمة المرور: `Admin123!`
  - تأكيد كلمة المرور: `Admin123!`
- [ ] الضغط على "إنشاء الحساب"
- [ ] **النتيجة المتوقعة**: رسالة "تم إنشاء الحساب بنجاح!"

#### 1.2 تسجيل الدخول (index.html)
- [ ] فتح `index.html`
- [ ] رؤية شاشة تحميل لثانية واحدة
- [ ] ملء النموذج:
  - البريد: `admin@test.com`
  - كلمة المرور: `Admin123!`
- [ ] الضغط على "تسجيل الدخول"
- [ ] **النتيجة المتوقعة**: التحويل التلقائي إلى `setup.html`

#### 1.3 إعداد المحل (setup.html)
- [ ] ملء معلومات المحل:
  - اسم المحل: "محل الخير"
  - رقم الهاتف: "07701234567"
  - العنوان: "بغداد - الكرادة"
  - اسم الوكيل: "أحمد محمد"
  - بادئة الفاتورة: "INV" (افتراضي)
- [ ] الضغط على "حفظ والمتابعة"
- [ ] **النتيجة المتوقعة**: التحويل التلقائي إلى `dashboard.html`

---

### المرحلة 2: لوحة التحكم (dashboard.html)

#### 2.1 التحقق من عرض البيانات
- [ ] رؤية شاشة تحميل قصيرة
- [ ] ظهور اسم المحل في الهيدر: "محل الخير"
- [ ] ظهور التاريخ الحالي
- [ ] البطاقات الإحصائية تظهر (كلها صفر في البداية):
  - فواتير اليوم: 0
  - مبيعات اليوم: 0 د.ع
  - أرباح اليوم: 0 د.ع
  - منتجات منخفضة المخزون: 0

#### 2.2 التحقق من القوائم
- [ ] القائمة الجانبية تظهر جميع الروابط
- [ ] الأزرار السريعة تعمل
- [ ] جدول "آخر الفواتير" يظهر "لا توجد فواتير بعد"
- [ ] جدول "منتجات منخفضة المخزون" يظهر "لا توجد منتجات"

---

### المرحلة 3: إدارة المنتجات (products.html)

⚠️ **ملاحظة**: هذه الصفحة تستخدم localStorage القديم حالياً. يجب تحديثها لاحقاً.

#### 3.1 الوصول للصفحة
- [ ] الضغط على "المنتجات" من القائمة
- [ ] **المشكلة المتوقعة**: قد تطلب تسجيل دخول (لأنها لم تُحدّث بعد)

---

### المرحلة 4: اختبار الجلسة

#### 4.1 استمرارية الجلسة
- [ ] إغلاق التبويب
- [ ] فتح `dashboard.html` مباشرة
- [ ] **النتيجة المتوقعة**: الدخول مباشرة بدون طلب تسجيل دخول

#### 4.2 تسجيل الخروج
- [ ] من `dashboard.html`، الضغط على "تسجيل الخروج"
- [ ] **المشكلة المتوقعة**: قد لا يعمل (يستخدم App.logout القديم)

---

## 🐛 المشاكل المتوقعة

### مشاكل معروفة يجب إصلاحها:

| المشكلة | الصفحة | الحالة |
|---------|--------|---------|
| تسجيل الخروج لا يعمل | dashboard.html | ⚠️ يستخدم App.logout القديم |
| صفحات المنتجات تطلب تسجيل دخول | products.html, customers.html, إلخ | ⚠️ تستخدم App.protectPage القديم |
| لا توجد بيانات تظهر | جميع الصفحات | ⚠️ localStorage vs Supabase |

---

## 📋 خطوات الاختبار السريع

### الاختبار الأساسي (5 دقائق)

```
1. افتح register.html
   → سجل حساب جديد

2. افتح index.html
   → سجل دخول

3. ستفتح setup.html تلقائياً
   → أدخل معلومات المحل

4. ستفتح dashboard.html تلقائياً
   → تحقق من ظهور اسم المحل

5. أغلق التبويب وافتح dashboard.html
   → يجب أن تدخل مباشرة (بدون تسجيل دخول)
```

### اختبار متقدم (إذا نجح الأساسي)

```
6. من dashboard، جرب الضغط على:
   - المنتجات
   - الزبائن  
   - الفواتير
   - الإعدادات
   
   → تحقق أيها يعمل وأيها يطلب تسجيل دخول
```

---

## ✨ ما يجب أن يعمل الآن

- ✅ التسجيل (register.html)
- ✅ تسجيل الدخول (index.html)
- ✅ إعداد المحل (setup.html)
- ✅ لوحة التحكم (dashboard.html)
- ✅ حفظ الجلسة (لا يطلب تسجيل دخول مرة أخرى)
- ✅ شاشات التحميل السلسة
- ✅ لا يوجد redirect loops

---

## ❌ ما لا يعمل بعد (يحتاج تحديث)

- ❌ تسجيل الخروج
- ❌ صفحة المنتجات
- ❌ صفحة الزبائن
- ❌ صفحة الفواتير
- ❌ صفحة التوريدات
- ❌ صفحة الإحصائيات
- ❌ صفحة الإعدادات

**السبب**: هذه الصفحات ما زالت تستخدم `App.protectPage()` و `localStorage` القديم.

---

## 🔧 المطلوب بعد الاختبار

إذا نجحت الخطوات 1-5، المرحلة التالية:

1. **تحديث باقي الصفحات** لاستخدام Supabase
2. **إصلاح تسجيل الخروج** ليستخدم `SupabaseDB.signOut()`
3. **ربط الصفحات بقاعدة البيانات** بدلاً من localStorage

---

## 📞 في حالة وجود مشاكل

**أرسل:**
1. اسم الصفحة التي لا تعمل
2. نص الخطأ (من Console - اضغط F12)
3. ما الذي حدث vs ما المتوقع

**مثال:**
```
الصفحة: dashboard.html
الخطأ: SupabaseDB is not defined
المتوقع: يجب أن تظهر البيانات
```
